import 'package:flutter/foundation.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  // Print the test name
  testWidgets('Test the main0.dart file', (WidgetTester tester) async {
    // Print the test name
    if (kDebugMode) {
      print("Test the main0.dart file");
    }
  });
}